general:
  branches:
    only: 
      - v3.0-fb
machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker pull centos
test:
  post:
    - docker run -t -i centos
    - yum install -y wget gcc gcc-g++ git make


